#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    int num,first,last,sum;
    printf("ENTER THE 4 DIGIT NUMBER\n");
    scanf("%d",&num);
    first=num/1000;
    last=num%10;
    sum=1st+last;
    printf("SUM OF 1ST AND LAST DIGIT IS %d\n",sum);
    getch();
}
